package com.example.blooddonation.ui.theme

/**
 * Compose color references removed as the project uses XML layouts.
 * These files are kept to prevent unresolved reference errors if imported elsewhere,
 * but are empty or simplified to avoid library dependency issues.
 */
class Color
